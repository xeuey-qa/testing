# 📊 Примеры SQL запросов для тестирования

Здесь представлены базовые запросы, которые я использую для проверки данных в базе при тестировании бэкенда.

---

### 🔎 1. Проверка регистрации пользователя
*Используется для подтверждения того, что новый пользователь успешно создался в БД.*
```sql
SELECT * FROM users WHERE email = 'nikita_test@gmail.com';
```

### 💰 2. Тестирование фильтра цен

*Проверка того, что в выдачу попадают только товары дороже определенной суммы.*
```sql
SELECT * FROM products WHERE price > 1000;
```
### 📦 3. Контроль статусов заказов

*Позволяет найти заказы, которые должны были уйти в доставку, но остались в статусе "Новый".*
```sql
SELECT * FROM orders WHERE status = 'new';
```


---

> **Примечание:** Эти запросы помогают убедиться, что данные корректно сохраняются в базе.